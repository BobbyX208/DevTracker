name: devtracker-api
service:
  type: web
  runtime: python
  buildCommand: pip install -r requirements.txt
  startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
  env:
    - key: FLASK_DEBUG
      value: "0"
    - key: DATABASE_URL
      sync: postgres
  regions:
    - fra
  instanceType: nano